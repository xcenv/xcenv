#!/bin/sh
# Summary: Rehashes the shims

set -e
[ -n "$XCENV_DEBUG" ] && set -x

for FILE_TO_SHIM in $(cd /usr/bin; grep -l "libxcselect.dylib" * 2>/dev/null)
do
	cp "${XCENV_ROOT}/libexec/.template" "${XCENV_ROOT}/shims/$(basename $FILE_TO_SHIM)"
done